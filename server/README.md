cd to this directory and then do:

```
pip install -r requirements.txt
```

To run the server:

cd into habit and do

```
python manage.py runserver
```

Visit localhost:8000/api in the browser to see the goods